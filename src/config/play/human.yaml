defaults:
  - base
  - _self_

# Human play specific configuration
play:
  mode: "human"

# Command detection
play_command: "human"

# Play-specific logging
logging:
  console:
    level: "INFO"
    progress_bars: false