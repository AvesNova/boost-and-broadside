defaults:
  - base
  - _self_

# BC data collection specific configuration
collect:
  mode: "bc"

# Command detection
collect_command: "bc"

# Data collection logging
logging:
  console:
    level: "INFO"
    progress_bars: true