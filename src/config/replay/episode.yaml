defaults:
  - replay/base

# Episode replay specific configuration
replay:
  mode: "episode"
  
# Command detection
replay_command: "episode"
  
logging:
  console:
    level: "INFO"
    progress_bars: false