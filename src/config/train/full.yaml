defaults:
  - ../base
  - _self_

# Full training pipeline specific configuration
training:
  mode: "full"
  rl:
    total_timesteps: 2000000
    
# Command detection
train_command: "full"
    
# Full pipeline logging
logging:
  console:
    level: "INFO"
    progress_bars: true