defaults:
  - base
  - _self_

# RL training specific configuration
training:
  mode: "rl"
  rl:
    total_timesteps: 2000000
    
# Command detection
train_command: "rl"
    
# RL-specific logging
logging:
  console:
    level: "INFO"
    progress_bars: true