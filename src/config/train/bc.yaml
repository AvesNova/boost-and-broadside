defaults:
  - ../base
  - _self_

# BC training specific configuration
training:
  mode: "bc"

# Command detection
train_command: "bc"

# BC-specific logging
logging:
  console:
    level: "INFO"
    progress_bars: true