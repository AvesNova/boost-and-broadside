# Base configuration for evaluation commands
# This file extends the base configuration with evaluation-specific defaults

# Inherits all defaults from base.yaml
defaults:
  - base
  - _self_

# Evaluation-specific configuration
# Note: Evaluation configuration is already defined in base.yaml as 'evaluation'